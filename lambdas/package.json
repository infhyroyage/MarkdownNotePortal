{
  "name": "mkmemoportal-lambdas",
  "version": "1.0.0",
  "description": "Lambda functions for Markdown Memo Portal",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --coverage --coverageReporters=text --coverageReporters=lcov",
    "test:ci": "NODE_OPTIONS=--experimental-vm-modules jest --coverage --coverageReporters=text --coverageReporters=lcov --coverageDirectory=./coverage",
    "lint": "eslint '**/*.js'"
  },
  "keywords": [
    "lambda",
    "aws",
    "dynamodb"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.713.0",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.13.0",
    "jest": "^30.2.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
